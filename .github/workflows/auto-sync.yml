name: Scheduled Sync

on:
  schedule:
    # 设定在每小时的第 25 分钟执行，避开拥堵
    - cron: '25 * * * *'
  workflow_dispatch: # 保持手动按钮，方便我们现在测试

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 必须显式声明写权限
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4

      - name: Sync upstream changes
        run: gh repo sync ${{ github.repository }} -b main
        env:
          # 将这里的 GITHUB_TOKEN 换成你刚才设置的密钥名
          GITHUB_TOKEN: ${{ secrets.SYNC_PAT }}
